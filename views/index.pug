extends layout

block content
  H1 Mini Message Board
  p Start adding new messages!

  button(type="button")
     - var url = 'new';
      a(href='/' + url) Add new message

  table
      tr
        th Name
        th Message
        th Date created

      each message in messages
        tr
          td= message.user
          td= message.text
          td= message.added

